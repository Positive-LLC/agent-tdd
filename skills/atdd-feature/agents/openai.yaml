interface:
  display_name: "Agent TDD — Feature"
  short_description: "Plan a feature as the Notes Agent"
policy:
  # Planning entry point (deferred); human-invoked only (`$atdd-feature`).
  allow_implicit_invocation: false
